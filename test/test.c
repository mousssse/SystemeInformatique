/*
    This is the test file
*/

int compute(int a, int d) {
  int b, c = a + d * 5;
  b = a;
  while (c > 0) {
    b = b + a * 4;
  }
  ;
  return b;
}

// this is the main
int main(void) {
  int a = (2 + (2 < 1));
  if (a < 3) {
    print(a);
  } else {
    int b = compute(a, 2 * a);
    print(b);
  }
  return 0;
}

void tutu(int b) {
  int e, d, f;
  if ((b > 4) || 1) {};
  return;
}